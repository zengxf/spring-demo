server:
  port: 9001

spring:
  application:
    name: server-a
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        health-check-interval: 15s
        instance-id: server-a
        # instance-id: server-a-${server.port}
        # service-name: server-a