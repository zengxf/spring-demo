spring:
  application:
    name: my-ser-1
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        namespace: my-test
        group: DEFAULT_GROUP
        enabled: true
      discovery:
        server-addr: 127.0.0.1:8848