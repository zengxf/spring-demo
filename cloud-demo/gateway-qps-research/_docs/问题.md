# 问题

#### Spring Cloud Gateway 网关响应 503

- Gateway 项目中必须包含了 LoadBalancer 依赖
- 没有这个依赖，Gateway 无法将 `lb://` 协议解析为具体的 IP 端口，也会导致 503。

```groovy
// 🔥 必须加上，否则 Nacos 实例发现后无法进行负载均衡转发
implementation "org.springframework.cloud:spring-cloud-starter-loadbalancer"
```

#### Spring Cloud Gateway - NoSuchMethodError

- Spring Cloud 与 Spring Framework 版本不兼容。
- Spring Cloud Gateway 4.1.6 对应的应该是 Spring Boot 3.2.x 或 3.3.x。

```groovy
plugins {
    id "org.springframework.boot" version "3.2.5" // 🔼 升级到 3.2.5
    // ...
}
ext {
    set('springCloudVersion', "2023.0.3")          // Spring Cloud 版本
    set('springCloudAlibabaVersion', "2023.0.1.2") // Spring Cloud Alibaba 版本
}
dependencies {
    // ...
    implementation group: "org.springframework.cloud", name: "spring-cloud-starter-gateway"
    // ...
}
// ...
```